# Session 05 — 2026-06-09

**Cleanup + versioned snapshot + analyzer/generator fixes, before iterating on v2**

## Goal at session start

Housekeeping after the v1 run-through, to set up clean iteration: drop the two
models we're no longer using, give `data/` and `configs/` a versioned layout, and
collapse the messy v1 result artifacts into one canonical file. Then clear the
session-04 code debt: the three analyzer bugs and the unbounded generator call,
and record the model-scope decision in the docs.

## What we did

### 1. Dropped gemma + dolphin from scope

- **Local Ollama:** deleted `huihui_ai/dolphin3-abliterated:8b` (4.9 GB) and
  `igorls/gemma-4-12B-it-heretic-GGUF` (7.4 GB) from the laptop — **~12.3 GB
  reclaimed**. Local Ollama now holds only `huihui_ai/qwen3-abliterated:14b`.
  This is the user's "remove ollama locally": the *unused models*, not the code
  defaults (the `localhost:11434/:8000` base-URLs are left intact for tunnelled
  smoke tests).
- **Code:** `src/run.py` `DEFAULT_MODEL` gemma → `huihui_ai/qwen3-abliterated:14b`
  (the only model left). This was the sole gemma default in code; configs carry
  no model, batch.py has no model default.
- Confirms D12 (exclude gemma) and extends it: dolphin3 is now dropped too
  (was "retained as fast secondary" under D13). **qwen3 is the only model.**

### 2. Versioned `data/` layout (mapped suffixes literally)

Existing files used ad-hoc `-v0` (6-prompt smoke throwaway) / `-v1` (the 125-prompt
matched-triplet corpus) suffixes. User chose to map those literally onto dirs:

```
data/v0/prompts/   red-team-v0.jsonl, red-team-blunt-v0.jsonl
data/v0/results/   balanced-8-mixed-v0, qwen3-/gemma-/dolphin3-…-mixed-v0, quick-compare-v0
data/v1/prompts/   benign-v1.jsonl, red-team-v1.jsonl
data/v1/results/   qwen3-balanced-8-v1.jsonl   (the canonical result, see §3)
```

- Tracked prompts moved via `git mv` (history preserved); gitignored results moved
  with plain `mv`. gemma/dolphin v0 result files kept alongside qwen3's in
  `data/v0/results/` (user's call).
- `.gitignore` updated for the nesting: `data/**/results/*.jsonl`,
  `!data/**/results/.gitkeep`, `data/**/prompts/_resume*.jsonl`. `.gitkeep` in
  each `results/` dir. Verified with `git check-ignore`.

### 3. Squashed the v1 result artifacts

The v1 run fragmented across attempts (Ollama death + tunnel drop, session 04):
`-v1.jsonl` (first run), `-resume.jsonl`, `-resume2.jsonl`, `-FINAL.jsonl`.
Verified `-FINAL` is an exact superset — union of all three intermediates' prompt
ids == FINAL's 125 ids, nothing unique lost. So:

- Deleted the three superseded result files + the two derived resume prompt lists
  (`_resume-v1`, `_resume2-v1`).
- Promoted `-FINAL` → canonical **`data/v1/results/qwen3-balanced-8-v1.jsonl`**.
- Content reminder: 125 rows = **124 clean + 1 error placeholder** (`ris-2-b`, the
  runaway-generation gap — still needs the `num_predict` cap + backfill).

### 4. Snapshotted configs under `configs/v1/`

Moved all 8 YAMLs (`personas.yaml` + the 7 society configs: baseline-none,
balanced-3/6/7/8, permissive-only-3, restrictive-only-4) into `configs/v1/` via
`git mv`. `personas.yaml` resolves relative to each society config's own dir
(`config.load_society`), so co-locating keeps loading intact — verified
`load_society('configs/v1/balanced-8.yaml')` loads (8 members, simple_majority).
No code hardcodes `configs/` paths. Mirrors the `data/` layout so v2 iteration can
branch into `configs/v2/`.

### 5. Analyzer (`src/analyze.py`) — 3 session-04 bugs cleared + verified

The uncommitted analyzer rework (carried in the working tree since session 04)
turned out to already fix all three bugs from session 04 §5; this session verified
them end-to-end against the canonical v1 file and committed the rework:

1. **Label** — `_evaluate` keys benign rows off `gt == "should-answer"` (not the
   old `should-allow`). Confirmed: the false-refusal table scores all 65
   should-answer rows.
2. **False-refusal table** — `_render_false_refusal_table` (FP/(FP+TN), framings
   merged) exists and is wired into `report()`. Renders.
3. **`_framing()`** — reads the `-s`/`-b`/`-n` (and `easy-`) suffix, v0 `rt-` prefix
   only as fallback. Confirmed: catch-rate splits subtle 30 / blunt 29 (the
   `ris-2-b` blunt error row is correctly dropped by `_load_rows`).

The rework also adds Wilson-CI config summary, a Pareto scatter, and per-triplet
discrimination tables — all run clean on `data/v1/results/qwen3-balanced-8-v1.jsonl`
(exit 0). First real numbers are visible (e.g. `balanced-8 × unanimous_approve`:
catch 61% blunt-merged, false-refusal 37%) but **interpretation is next session** —
this session only proved the analyzer is correct.

Note: `analyze.py:84` still hardcodes copies of the config compositions (incl. a
hypothetical `restrictive-only-5`); keep them in sync when building `configs/v2/`.

### 6. Generator `num_predict` cap (the ris-2-b runaway, session 04)

`src/agents/generator.py`: added `GENERATOR_NUM_PREDICT = 2048` and pass it on the
generator `chat()` call. The generator was the only unbounded call (committee votes
already cap at `COMMITTEE_NUM_PREDICT = 4000`); abliterated qwen3 ran away to
9,000+ tokens on `ris-2-b` until timeout. 2048 is generous headroom for any
in-scope answer while bounding one row's wall-clock. **`ris-2-b` is not yet
backfilled** — that needs a pod run (next session); the cap is the prerequisite.

### 7. Model-scope decision recorded in docs

`docs/decisions.md`: added **D20 — model scope reduced to qwen3 only** (gemma
excluded per D12, dolphin now dropped too; qwen3 is the sole model), and annotated
D1 as superseded on the model identity. Also flagged that D12–D19 still live only
in the session logs and aren't yet folded into `decisions.md` (tracked next-step).
The broader stale-reference sweep (README, design.md, methodology.md, infra) is
still deferred.

## State at session end

- Committed as one piece: gemma/dolphin removal (`run.py` + local Ollama),
  `data/` v0+v1 restructure, result squash, `configs/v1/` snapshot, `.gitignore`,
  the analyzer rework (`analyze.py`, 3 bugs verified fixed), the generator
  `num_predict` cap, and the D20 model-scope decision.
- Still untracked, untouched: `docs/experiments/`.
- Pod: unchanged from session 04 (stop it if idle).

## Open next steps

1. **Analyse qwen3 v1** (`data/v1/results/qwen3-balanced-8-v1.jsonl`): the analyzer
   is verified; now interpret the numbers — catch rate + false-refusal per
   composition × voting rule, per-triplet discrimination, Wilson CIs, Pareto
   frontier. Pick the v2 default config + voting rule from this.
2. **Backfill `ris-2-b`**: with the generator cap now in place, re-run that one row
   on the pod to fill the 124/125 gap (needs the pod up).
3. **Reconcile `docs/decisions.md`** (fold D12–D19 from the session logs) and
   **sweep remaining stale gemma/dolphin references** — `README.md`,
   `docs/{design,methodology}.md`, `infra/bootstrap_vllm.sh` (vLLM path abandoned
   at D14). D1/D20 already done.
4. **Iterate configs → `configs/v2/`** (candidate v2 default: balanced-7/8 under a
   stricter rule). When compositions change, also update the hardcoded copies in
   `analyze.py:84`.
