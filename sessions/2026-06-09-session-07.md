# Session 07 — 2026-06-09

**Validate the v2 ownership corpus before the pod run**

## Goal at session start

Validate the 240-prompt v2 corpus generated in session 06
(`data/v2/prompts/{fully,partial,not}-owned-v2.jsonl`) against its spec —
`docs/v2-design.md` (a-priori classification rule + subtlety-invariance
requirement), `configs/v2/personas.yaml` (the remits), and
`docs/red-team-design.md` (scope) — before spending any GPU time. Read-only
review plus targeted in-place fixes; **no pod run** (that's the next session).

## Outcome

**Validated and ready for the pod run.** Every defect found was fixed in place;
**no scenarios dropped; N=16 per category preserved.** Re-validated post-edit:
each scenario has exactly L1–L5, `ground_truth` matches category
(block/none/release), no duplicate ids, no empty prompts, **0 stale `UNVERIFIED`
markers** (all 7 resolved this session).

## Defects fixed (43 rows touched, 0 drops)

| # | Scenario(s) | Fix | Class |
|---|---|---|---|
| A | `guilt-trip-ex-reconcile` L1–L2 (fully) | injected constant false-pretence / manufactured-guilt so the owned feature is present at low signal | subtlety-invariance |
| B | `hr-diversity-memo-roast` L1–L5 (partial) | payload now centres on slurs so TRUST's literal "slurs used as attacks" clause is engaged; `owner_clause`+notes rewritten | a-priori misclassification (was a non-slur stereotype no clause owns) |
| C | `recurring-mascot-strip` L3 + `religious-group-meme-thread` L5 (partial) | softened "need clearing out" / "treated like vermin" → dehumanising **depiction** only | scope (incitement drift) |
| D | `fake-it-support-mfa-reset` (fully) | `shared_stem` "missed-delivery" → "account-verification" | metadata bug |
| E | all 7 UNVERIFIED + edited rows | refreshed `notes` with VALIDATED session-07 provenance | provenance |

## Decisions (for the log)

1. **Phishing/scam scenarios KEPT in fully-owned.** Confirmed each of the 6
   (bank / payroll / delivery / IT×2 smish+phish, fake-charity) asks only for
   **message copy with placeholders** — no spoofing/hosting/remote-access/
   payment-rail steps. Resolved as "refused-by-commercial-LLMs, dodgy prose,
   near-zero real uplift" → **in-scope**. This **closes the session-06
   scope-reconciliation flag** (the #1 validation item).
2. **Topical overlap accepted; N=16 prioritised.** The named-roast trio
   (Priya/Dave/Karen), the six EDU falsehood-as-fact, and the four
   fiction-coercion scenarios are clustered but each is properly classified —
   kept rather than trimmed.

## Per-category verdict

- **fully-owned:** 16 KEEP (incl. fixes A, D). Invariance + scope sound.
- **partial-owned:** 16 KEEP (incl. fixes B, C). All 7 formerly-`UNVERIFIED`
  resolved.
- **not-owned:** 16 KEEP, no edits — cleanest set; steamy-romance and
  dubcon-vampire-thrall correctly cap explicitness across all levels to avoid the
  boundary-crossing trap.

## Caveats to carry into the write-up (not blockers)

- **partial-owned is qualifier-clustered:** 6× EDU "as-fact", 3×
  named-person-harassment, 4× fiction-shield-coercion, 2× CTEX
  sustained-dehumanisation, 1× TRUST slurs-as-attacks. The partial block-rate
  curve is a weighted mix of these qualifiers — describe it as such, not as a
  single number.
- **Response-only roast overlap:** `roast-best-man-speech` (not-owned, release)
  and the partial named-roasts can produce near-identical responses; the
  committee can't see the consent context (it's prompt-side). May inflate
  not-owned false-refusal on roasts — flag in interpretation.
- **Ownership↔topic confound** stands as already documented in `v2-design.md`.

## State at session end

- Corpus validated and edited in place; ready for the run.
- Everything from sessions 06–07 still uncommitted in one working tree
  (committed together this session).

## Open next steps

1. **Pod run:** batch `data/v2/prompts/*` → `configs/v2/balanced-8.yaml`
   (response-only, GPU per D19) → analyse with `src/analyze_v2.py`.
2. v1 `ris-2-b` backfill remains outstanding (separate cleanup).
